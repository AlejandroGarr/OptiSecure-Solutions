================================================================================
  OPTISECURE SOLUTIONS — AUDITORÍA DE LIMPIEZA PARA SUBIR A GITHUB
================================================================================
  Fecha: 19/05/2026
  Objetivo: identificar qué clases / LWCs / flujos / objetos del repositorio
  pertenecen al proyecto OptiSecure (mantener) y cuáles son restos de
  ejercicios o scaffolding por defecto (candidatos a borrar).
  IMPORTANTE: nada se ha borrado todavía. Esto es solo el inventario.
================================================================================


================================================================================
  1. ESTRUCTURA DE CARPETAS (force-app/main/default)
================================================================================

force-app/main/default/
├── classes/                       ← Apex (controladores + tests)
├── triggers/                      ← VACÍO (no hay triggers)
├── lwc/                           ← Componentes Lightning Web
│   ├── camara/
│   ├── cameraContractRequest/
│   ├── cameraIncidentReport/
│   ├── caseChatPanel/
│   ├── caseViewer/
│   ├── clipNotifications/
│   ├── contractApproval/
│   ├── contractHistory/
│   ├── customUserMenu/
│   ├── houseSecurityMap/         ← componente principal (mapa interactivo)
│   ├── letter/                   ← (vacío)
│   ├── map/                      ← (vacío)
│   ├── pubsub/                   ← (utilidad no importada)
│   ├── securityClipViewer/
│   ├── voiceAssistant/
│   └── webCam/                   ← (vacío)
├── aura/                          ← Componentes Aura (solo login Experience Cloud)
│   ├── forgotPassword/
│   ├── loginForm/
│   ├── selfRegister/
│   ├── setExpId/
│   └── setStartUrl/
├── flows/
│   ├── Reportar_Incidencia_Camara.flow-meta.xml   ← OptiSecure
│   ├── Get_Case_Comments.flow-meta.xml            ← OptiSecure / Cases
│   ├── FlowScreenCualificacion.flow-meta.xml      ← demo (revisar)
│   ├── Factura_no_cobrada.flow-meta.xml           ← demo ejercicio
│   ├── feliz_Cumple.flow-meta.xml                 ← demo ejercicio
│   ├── FlowHotCold.flow-meta.xml                  ← demo ejercicio
│   └── OportunidadFlow.flow-meta.xml              ← demo ejercicio
├── objects/                       ← cientos de carpetas estándar (no tocar)
│   ├── Camara__c/                ← núcleo OptiSecure
│   ├── Solicitud_Camara__c/      ← núcleo OptiSecure
│   ├── Deteccion_Persona__e/     ← Platform Event OptiSecure
│   ├── Cuenta__c/                ← demo (duplicado de Account)
│   ├── Contacto__c/              ← demo (duplicado de Contact)
│   ├── Factura__c/               ← demo
│   ├── Oportunidad__c/           ← demo
│   └── Producto_de_Oportunidad__c/  ← demo
├── staticresources/
│   ├── Plano_Casa_1.(jpeg|png)   ← OptiSecure (plano por defecto)
│   ├── ImagenCliente.png         ← demo (FlowScreenCualificacion)
│   ├── SiteSamples/              ← scaffolding por defecto
│   └── VoiceCaptureHtml.html     ← antiguo, no referenciado
├── digitalExperiences/            ← OptiSecure Solutions (Experience Cloud)
├── networks/                      ← OptiSecure Solutions network
├── sites/                         ← OptiSecure site
├── flexipages/                    ← record pages
├── layouts/, profiles/, permissionsets/, permissionsetgroups/  ← metadata estándar
├── tabs/, applications/, quickActions/, labels/, etc.
└── (resto de carpetas que SF genera vacías o casi vacías)


================================================================================
  2. APEX — CLASES QUE SE USAN (MANTENER)
================================================================================

Controladores núcleo OptiSecure:
  ✔ CameraIncidentController        → usado por lwc/cameraIncidentReport
  ✔ CameraInsertHelper              → helper sin sharing invocado por
                                       ContratacionCamaraController
  ✔ CaseViewerController            → usado por lwc/caseViewer y caseChatPanel
  ✔ ContratacionCamaraController    → usado por lwc/cameraContractRequest y
                                       contractHistory
  ✔ FloorPlanController             → usado por lwc/customUserMenu y
                                       houseSecurityMap
  ✔ HouseMapController              → usado por lwc/houseSecurityMap
  ✔ PersonDetectionController       → endpoint REST para el detector YOLO
                                       (servicio Python externo). Crea Tasks
                                       a partir del Platform Event
                                       Deteccion_Persona__e.
  ✔ SecurityClipController          → usado por lwc/clipNotifications y
                                       securityClipViewer
  ✔ UserMenuController              → usado por lwc/customUserMenu y
                                       houseSecurityMap
  ✔ VoiceCommandController          → usado por lwc/voiceAssistant

Tests asociados (MANTENER, dan cobertura a los controladores núcleo):
  ✔ CameraIncidentControllerTest
  ✔ ContratacionCamaraControllerTest
  ✔ HouseMapControllerTest
  ✔ VoiceCommandControllerTest


================================================================================
  3. APEX — CLASES CANDIDATAS A BORRAR
================================================================================

A. Placeholder vacío:
  ✗ AccountController               → la clase está literalmente vacía
                                       (solo constructor sin lógica). No la
                                       referencia nadie.

B. Scaffolding por defecto de Experience Cloud / Site.com
   (Salesforce los genera al crear el sitio; el proyecto NO los personaliza,
    así que se pueden eliminar SIEMPRE QUE no hayas configurado en el
    Experience Builder páginas Visualforce/Aura clásicas que los usen):
  ✗ ChangePasswordController              + ChangePasswordControllerTest
  ✗ CommunitiesLandingController          + CommunitiesLandingControllerTest
  ✗ CommunitiesLoginController            + CommunitiesLoginControllerTest
  ✗ CommunitiesSelfRegConfirmController   + CommunitiesSelfRegConfirmControllerTest
  ✗ CommunitiesSelfRegController          + CommunitiesSelfRegControllerTest
  ✗ ForgotPasswordController              + ForgotPasswordControllerTest
  ✗ LightningForgotPasswordController     + LightningForgotPasswordControllerTest
  ✗ LightningLoginFormController          + LightningLoginFormControllerTest
  ✗ LightningSelfRegisterController       + LightningSelfRegisterControllerTest
  ✗ MicrobatchSelfRegController           + MicrobatchSelfRegControllerTest
  ✗ MyProfilePageController               + MyProfilePageControllerTest
  ✗ SiteLoginController                   + SiteLoginControllerTest
  ✗ SiteRegisterController                + SiteRegisterControllerTest

   NOTA: si tu sitio sigue usando el login estándar de Salesforce
   ("Login Page" del Experience Builder con plantillas clásicas), conviene
   conservar al menos LightningLoginFormController y LightningSelfRegisterController.
   Los demás son seguros de borrar en casi cualquier escenario moderno.


================================================================================
  4. LWC — COMPONENTES QUE SE USAN (MANTENER)
================================================================================

  ✔ camara                  → galería de cámaras / streams
  ✔ cameraContractRequest   → solicitar contratación de cámara
  ✔ cameraIncidentReport    → modal de reporte de incidencias
                              (lanzado por el flujo Reportar_Incidencia_Camara)
  ✔ caseChatPanel           → panel de comentarios dentro de caseViewer
  ✔ caseViewer              → panel/dashboard de casos
  ✔ clipNotifications       → notificación de clips de seguridad
  ✔ contractApproval        → UI admin para aprobar/denegar solicitudes
  ✔ contractHistory         → historial de contrataciones del usuario
  ✔ customUserMenu          → menú lateral de perfil + gestión de cámaras
  ✔ houseSecurityMap        → MAPA INTERACTIVO PRINCIPAL (Plano_Casa,
                              drag&drop de chinchetas, modal del stream)
  ✔ securityClipViewer      → reproductor de clips guardados
  ✔ voiceAssistant          → asistente por voz (Web Speech API +
                              VoiceCommandController)


================================================================================
  5. LWC — CANDIDATOS A BORRAR
================================================================================

  ✗ letter   → componente vacío (sin template ni lógica).
  ✗ map      → componente vacío con comentario "prueba commit 2" — prueba
               del repositorio, no se usa en ningún sitio.
  ✗ webCam   → componente vacío, sin lógica de webcam real.
  ✗ pubsub   → módulo helper de pub/sub. NO está importado por ningún otro
               LWC del repo. Si nunca lo importas, se puede eliminar.


================================================================================
  6. AURA — BUNDLES
================================================================================

Todos los bundles Aura presentes son del flujo de autenticación del
Experience Cloud (login / registro / recuperación de contraseña):

  ◉ loginForm        → asociado a LightningLoginFormController
  ◉ forgotPassword   → asociado a ForgotPasswordController
  ◉ selfRegister     → asociado a (Lightning)SelfRegisterController
  ◉ setExpId         → set Experience ID (utilidad de login)
  ◉ setStartUrl      → set Start URL (utilidad de login)

  → Si decides borrar los controladores Apex de la sección 3.B, BORRA
    también el bundle Aura asociado (van en pareja). Si mantienes el login
    estándar del sitio, conserva como mínimo loginForm + setExpId + setStartUrl
    junto con sus controladores LightningLoginFormController y
    LightningSelfRegisterController.


================================================================================
  7. FLOWS
================================================================================

MANTENER (núcleo OptiSecure):
  ✔ Reportar_Incidencia_Camara   → flujo principal de incidencias; abre
                                    el LWC cameraIncidentReport.
  ✔ Get_Case_Comments            → consulta de comentarios de casos
                                    (lo usa caseViewer/caseChatPanel).

REVISAR (ligado a Account estándar, no a OptiSecure):
  ⚠ FlowScreenCualificacion      → flujo de cualificación de cuentas;
                                    referenciado en Account.Cualifica
                                    (quickAction) y en Account_Record_Page
                                    (flexipage). Si quieres que el repo
                                    quede SOLO OptiSecure, bórralo junto
                                    con esa quick action y esa sección del
                                    flexipage. Si lo dejas, es un extra
                                    inofensivo.

BORRAR (ejercicios CRM antiguos, nada que ver con cámaras):
  ✗ Factura_no_cobrada           → recordatorio de facturas impagadas.
  ✗ feliz_Cumple                 → email de cumpleaños a Contacto__c.
  ✗ FlowHotCold                  → clasificación caliente/frío sobre
                                    Account + Oportunidad__c.
  ✗ OportunidadFlow              → crea Factura__c desde una oportunidad.


================================================================================
  8. OBJETOS CUSTOM (__c / __e)
================================================================================

MANTENER (núcleo OptiSecure):
  ✔ Camara__c                    → cámara de seguridad.
  ✔ Solicitud_Camara__c          → solicitud de contratación.
  ✔ Deteccion_Persona__e         → Platform Event de detección.

BORRAR (ejercicios CRM en español, duplican objetos estándar):
  ✗ Cuenta__c                    → duplicado de Account, solo lo usan los
                                    flujos demo.
  ✗ Contacto__c                  → duplicado de Contact, solo lo usa
                                    feliz_Cumple.
  ✗ Factura__c                   → solo lo usa OportunidadFlow y
                                    Factura_no_cobrada.
  ✗ Oportunidad__c               → solo lo usa FlowHotCold.
  ✗ Producto_de_Oportunidad__c   → línea de Oportunidad__c.

  → Al borrar estos objetos recuerda también limpiar los layouts,
    permission sets, profiles y objectTranslations asociados (search
    "Cuenta__c", "Contacto__c", "Factura__c", "Oportunidad__c",
    "Producto_de_Oportunidad__c" en force-app y elimina lo que aparezca).


================================================================================
  9. STATIC RESOURCES
================================================================================

MANTENER:
  ✔ Plano_Casa_1.jpeg / Plano_Casa_1.png   → plano por defecto que carga
                                              houseSecurityMap.

REVISAR / BORRAR:
  ⚠ ImagenCliente.png            → solo se usa dentro del flujo
                                    FlowScreenCualificacion. Si borras
                                    ese flujo, bórralo también.
  ✗ VoiceCaptureHtml.html        → no está referenciado por ningún LWC
                                    ni Apex actual (versión antigua del
                                    captador de voz; ahora se hace dentro
                                    del LWC voiceAssistant con Web Speech).
  ✗ SiteSamples                  → carpeta de samples por defecto que
                                    crea Salesforce; nada propio.


================================================================================
  10. RESUMEN — ACCIONES RECOMENDADAS ANTES DE SUBIR A GITHUB
================================================================================

Mínimo recomendado (limpieza segura sin afectar al sitio):
  1. Borrar el LWC `pubsub`, `letter`, `map`, `webCam`.
  2. Borrar la clase Apex `AccountController`.
  3. Borrar los flujos `Factura_no_cobrada`, `feliz_Cumple`, `FlowHotCold`,
     `OportunidadFlow` y, asociados, los objetos `Factura__c`,
     `Oportunidad__c`, `Producto_de_Oportunidad__c`, `Contacto__c`,
     `Cuenta__c` (junto con sus layouts / object translations / refs en
     profiles y permissionsets).
  4. Borrar el static resource `VoiceCaptureHtml` y la carpeta `SiteSamples`.

Limpieza adicional (si NO usas el login clásico Aura del sitio):
  5. Borrar las parejas Apex + Test marcadas en la sección 3.B
     (Community*, Lightning*Controller, MicrobatchSelfReg*, MyProfilePage*,
     Site(Login|Register)*, ChangePassword*, ForgotPassword*).
  6. Borrar los bundles Aura asociados en `aura/` que ya no usen esos
     controladores.

Limpieza opcional (deja el repo 100% OptiSecure, sin demos de cuentas):
  7. Borrar el flujo `FlowScreenCualificacion`, el quick action
     `Account.Cualifica`, la sección correspondiente en
     `Account_Record_Page.flexipage`, y el static resource
     `ImagenCliente.png`.

================================================================================
  FIN DEL INFORME
================================================================================
